# ChatGPT icon variants — 2026-09-07

Status: local assets verified. Scope: local icon assets and OpenAI package references only.
Source: owner's screenshot and request for a #F1F5F9 dark-mode icon and exact
48x48 composer icon. The screenshot requires directory PNGs of at least 256x256.
No portal upload, publication, credential access, backend or other-host change.

## Requirements and verification

All requirements are must-have and in scope; the user request is their authority.

| ID | Parent | Requirement / decision | Verification and expected result |
|---|---|---|---|
| PRD-ICON-001 | User request | The package shall provide usable ChatGPT dark-mode and composer icons. | T-ICON-VISUAL: inspect the final files at intended sizes; portal rendering is separate and not verified. |
| PROD-ICON-001 | PRD-ICON-001 | The dark directory PNG shall use the existing mark in #F1F5F9 on transparency. | T-ICON-PIXELS: 1024x1024 RGBA; every visible RGB pixel equals (241,245,249); transparent corners. |
| PROD-ICON-002 | PRD-ICON-001 | The composer PNG shall be exactly 48x48. | T-ICON-PIXELS: RGBA, visible original mark, transparent corners, dark brand #334155. |
| ARCH-ICON-001 | PROD-ICON-001, PROD-ICON-002 | The variants shall derive from the canonical native SVG and enter the generated OpenAI package through its adapter. | T-ICON-PACKAGE: original source/other-host icons preserved; generated bytes match adapter; manifest paths resolve. |
| SYS-ICON-001 | ARCH-ICON-001 | Package validation shall reject missing or incorrectly sized icon variants. | T-ICON-NEGATIVE: alter manifest or PNG dimensions in isolated tests and observe rejection. |
| IMPL-ICON-001 | SYS-ICON-001 | The manifest shall reference distinct dark-directory and composer assets. | T-ICON-PACKAGE: logoDark and composerIcon point to the new files; deterministic bundle contains both. |

## Architecture and catalog disposition

Existing logoDark points to the same dark-ink icon as logo. Existing composerIcon
is a valid transparent 192px image, but the owner needs a dedicated 48px upload.
Use the native SVG's rocket/rays, excluding its background and wordmark. Rasterize
with Sharp; no AI redrawing, new design, network call or font dependency. Preserve
existing assets and add named siblings. Sharp is build-only; checked-in PNGs keep
Python-only validation/install unchanged. Reverting the two manifest references
restores prior behavior without deleting any source asset.

Catalog: product/functional, architecture/contrast, compatibility and verification
apply through the matrix. Performance N/A beyond fixed small file dimensions;
no runtime or load behavior changes. Security/privacy: public brand assets only,
no credentials or external writes. Data migration, localization and service
availability N/A because this is static branding. Maintain pending release gates
and refresh candidate digests; do not reuse prior installation evidence for the
changed candidate.

Assumption: the single composer slot uses dark #334155, matching existing branding;
the user specified #F1F5F9 for the dark-mode directory slot. Portal upload is not
authorized by the earlier inspect-only approval and is not claimed here.

## Evidence

T-ICON-PIXELS passed using the builder's decoded-PNG checks: exact RGB, four
transparent corners, nonempty alpha and exact dimensions. Native SVG paths remain
unchanged. Visual inspection of a 256px dark-background directory preview and
native-size 48px composer on white passed (`dist/chatgpt-icons-preview.png`).
The code-simplifier pass kept explicit rasterization, color normalization and
encoded-output verification in the small build-only script.

T-ICON-PACKAGE and T-ICON-NEGATIVE: four focused tests passed, including changed
dimension rejection and deterministic ZIP completeness. Repository host/submission
validators and pending release-evidence checks passed. Generated file count is
462; other-host archive hashes are unchanged. All publication gates stay pending.
The new OpenAI archive digest is
`72D4F4893D86D7BA198FA0A0E8345017CCFB6F476441E847EA43D6BC943B982F`.

The bundled generic plugin validator rejects the existing `.mcp.json` top-level
`sparklaunch` envelope and expects `mcpServers`. This unchanged pre-existing format
is outside the icon request; the repository's own validator accepts it. No MCP
format or authentication changes were made to silence that separate check.
T-ICON-VISUAL establishes local asset readiness, not a portal upload or published
appearance. The earlier inspect-only portal boundary remains in force.

Final regression: 363 passed, six Windows symlink-privilege skips. Python compile,
JavaScript syntax, generated parity and diff whitespace checks passed. All traced
local asset requirements are satisfied; portal rendering remains NOT VERIFIED.
